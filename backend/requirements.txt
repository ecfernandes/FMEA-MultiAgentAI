# ============================================================================
# backend/requirements.txt
# Dependencies for the FastAPI backend ONLY.
# Heavy ML libraries (torch, transformers, spacy, etc.) live in the root
# requirements.txt and are NOT needed inside the API container.
# ============================================================================

# API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# HTTP client (used by specialist_agents.py)
httpx>=0.27.0

# LLM client (UTC platform / OpenAI-compatible)
openai>=1.0.0

# PDF extraction (fmea_pdf_extractor.py uses fitz)
pymupdf>=1.23.0

# Excel extraction (fmea_extractor_v2.py uses pandas + openpyxl)
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1        # required for legacy .xls format

# Pydantic (FastAPI depends on it; pin to v2-compatible range)
pydantic>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Vector store / RAG (book indexer)
chromadb>=0.5.0
